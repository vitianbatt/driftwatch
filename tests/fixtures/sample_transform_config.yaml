# sample_transform_config.yaml — example field transform configuration
transforms:
  - field: environment
    transform: lowercase
  - field: region
    transform: uppercase
  - field: service_name
    transform: strip
